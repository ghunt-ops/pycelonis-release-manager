from unittest.mock import patch

from pycelonis.utils.yaml import dump_yaml


class TestYaml:
    @patch("yaml.dump")
    def test_dump_yaml(self, mock_dump_yaml):
        DICTIONARY = {"TEST": "TEST"}

        dump_yaml(DICTIONARY)

        mock_dump_yaml.assert_called_once_with(DICTIONARY, sort_keys=False)
